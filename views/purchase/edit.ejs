<!DOCTYPE html>
<body class="container mt-5">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <h2>Edit Purchase Request: <%= pr.pr_number %></h2>
    <form action="/purchase/<%= pr.id %>?_method=PUT" method="POST">
        <input type="text" name="item_name" class="form-control mb-2" value="<%= pr.item_name %>">
        <input type="number" name="quantity" class="form-control mb-2" value="<%= pr.quantity %>">
        <input type="text" name="unit" class="form-control mb-2" value="<%= pr.unit %>">
        <select name="status" class="form-control mb-2">
            <option value="Pending" <%= pr.status=='Pending'?'selected':'' %>>Pending</option>
            <option value="Approved" <%= pr.status=='Approved'?'selected':'' %>>Approved</option>
            <option value="Rejected" <%= pr.status=='Rejected'?'selected':'' %>>Rejected</option>
        </select>
        <button class="btn btn-primary">Update</button>
    </form>
</body>